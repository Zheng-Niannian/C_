/*Дано целое число, не меньшее двух. Вывести:
1) его наименьший натуральный делитель, отличный от 1;
2) список всех натуральных делителей данного числа; 
3) «простое», если число является таковым и «не является простым» иначе.
*/

#include <stdio.h>

int main() {
    int num;
    printf("Введите целое число (не меньше двух): ");
    scanf("%d", &num);

    int a = 2;  
    int b = 1;  

    printf("Наименьший натуральный делитель, отличный от 1: ");
    
    while (a <= num) {
        if (num % a == 0) {
            printf("%d\n", a); 
            b = 0;  
            break;  
        }
        a++;
    }

    if (b) {
        printf("Простое\n");
    } else {
        printf("Не является простым\n");
    }

    printf("Список всех натуральных делителей данного числа:\n");
    for (int i = 1; i <= num; i++) {
        if (num % i == 0) {
            printf("%d\n", i);  
        }
    }

    return 0;
}
