/*
"Вложенные операторы if-else и целочисленное деление"
Задание: создать нижеописанные программы с использованием вложенных операторов if-else; 
в каждой программе реализовать проверку введённого числа на соответствие требуемому диапазону.
Подсказка: Для получения цифр числа используйте операторы «/» и «%».
Пользователь вводит четырёхзначное число. 
Программа должна вывести наибольшую цифру этого числа и наименьшую цифру этого числа. 
Поиск наибольшей и наименьшей цифр организовать с помощью вложенных операторов if-else.
*/

#include <stdio.h>

int main() {
    int number;
    printf("Пожалуйста, введите четырехзначный номер: ");
    scanf("%d", &number);

    if (number < 1000 || number > 9999) {
        printf("Введенное число не состоит из четырех цифр.\n");
    }
    else {
        int largestdigit = -1; 
        int smallestdigit = 10; 

        int temp = number;

        while (temp > 0) {
            int digit = temp % 10; 
            if (digit > largest_digit) {
                largest_digit = digit;
            }
            if (digit < smallest_digit) {
                smallest_digit = digit;
            }
            temp /= 10; 

        printf("Максимальное количество: %d\n", largest_digit);
        printf("Минимальное количество: %d\n", smallest_digit);
    }

    return 0;
}
